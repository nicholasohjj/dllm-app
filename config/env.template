# VAPID Keys for Web Push Notifications
# Copy this file to .env in the root directory and fill in your values
# Run 'npm run generate-vapid' to generate your own VAPID keys

# ====================================
# Frontend Environment Variables
# ====================================

# Public key (used in the frontend - safe to expose)
VITE_VAPID_PUBLIC_KEY=your-public-key-here

# Lambda URL (for fetching machine data)
VITE_REACT_APP_LAMBDA_URL=your-lambda-url-here

# ====================================
# Backend Environment Variables
# (Only needed if you're running backend API)
# ====================================

# Private key (KEEP SECRET - used in the backend/API)
VAPID_PRIVATE_KEY=your-private-key-here

# Subject (your email or website URL)
VAPID_SUBJECT=mailto:your-email@example.com

# ====================================
# Setup Instructions:
# ====================================
# 1. Copy this file to .env in the root directory
# 2. Run 'npm run generate-vapid' to generate VAPID keys
# 3. Update VAPID_SUBJECT with your email or website URL
# 4. Update VITE_REACT_APP_LAMBDA_URL with your actual Lambda URL
# 5. Make sure .env is in your .gitignore

